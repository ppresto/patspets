
#  CLI Usage to support a configurable backend at runtime
#
# terraform init -backend-config=backend-master.hcl
#
#  Note:  Getting Error:  workspaces should be a list.   Documentation shows this should be a map.  Appears to be a bug with TF 11.14


terraform {
    backend "remote" {}
}